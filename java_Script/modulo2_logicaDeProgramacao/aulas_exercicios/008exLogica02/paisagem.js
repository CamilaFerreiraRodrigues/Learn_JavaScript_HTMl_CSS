/* escreva uma função chamada de epaisagem
que recebe dois argumentos, largura e altura de uma imagem  (number. Retorne true se a imagem estiver no mooo paisagem)*/

const ePaisagem = (largura, altura) => largura > altura //? true : false;

    //DICA: ñ precisa escrever true/ false pq a condição já faz isso.

console.log(ePaisagem(1920,1080));